events {}

http {
    server {
        listen 443 ssl;
        ssl_certificate_key /etc/ssl/private/api.key;

        location / {
            proxy_set_header Authorization "Bearer s3cr3t-token";
            proxy_set_header X-Masked-Upstream ***;
            proxy_pass http://backend;
        }
    }
}
